# Changelog

All notable changes to `ai-orchestrator-client` are recorded here.
Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/);
versions follow [PEP 440](https://peps.python.org/pep-0440/).

## [Unreleased]

### Added
- Initial repository skeleton (Phase A): `pyproject.toml`, Apache-2.0
  `LICENSE`, package directory `ai_orchestrator_client/` with `py.typed`
  marker, smoke test, ruff + mypy `--strict` config, GitHub Actions CI
  on Python 3.11 + 3.12.
